# 从 FastAPI 到 Agent 工程师 —— 8 周学习计划

> 🧑‍🏫 老师：ZCode ｜ 👨‍🎓 学生：你
> 起始日期：2026-06-17（暑假）｜ 节奏：每天 2-3 小时有效学习
> 核心原则：**边学边做，每个知识点配一个小项目，做完再学下一个。不追求"学完"，追求"做出来"。**

---

## 〇、学习心法（先读这一段，比后面的任务更重要）

1. **别"看完文档再动手"**：官方文档每个例子都敲一遍、改一改，比"通读"有用 10 倍。
2. **小步快跑**：每个 Step 都让你立刻看到结果（哪怕只是终端里打印一行）。
3. **遇到不懂的概念先问 AI 老师，再搜文档**：节省时间，别在某个名词上卡半天。
4. **每个项目都要 Git commit + 推到 GitHub**：8 周后你会有 5+ 个仓库，这是你简历上最硬的东西。
5. **写"学习笔记 + 踩坑记录"**：每周一次，讲清楚 = 真懂了。

---

## 一、技能全景图（你 8 周后会点亮这些）

```
                         [ 你现在的位置 ]
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
   Phase 1-2               Phase 3-4               Phase 5-6
    FastAPI                LLM + RAG             Agent + 前端
        │                      │                      │
   Python进阶             DeepSeek API         Function Calling
   路由/校验               Embedding            ReAct 循环
   pydantic               向量库 Chroma          多步任务编排
   异步 async              Prompt 工程           Streamlit 界面
   依赖注入                                      (后续) JS/TS→React
   数据库
```

| 阶段 | 主题 | 时长 | 产出项目 | 是否核心 |
|------|------|------|----------|----------|
| Phase 0 | 环境搭建 + Git 热身 | 1-2 天 | 仓库 + Hello World | ✅ |
| Phase 1 | FastAPI 基础 | 1 周 | 个人记账 API（内存版） | ✅ |
| Phase 2 | FastAPI 进阶 + 数据库 | 1 周 | 个人记账 API（持久化 + 统计版） | ✅ |
| Phase 3 | LLM 接入 + Prompt 工程 | 1 周 | 翻译 API / 智能客服 | ✅ |
| Phase 4 | RAG 知识库 | 1 周 | 文档问答机器人 | ✅ |
| Phase 5 | ⭐ Agent 核心 | 1.5 周 | 会用工具的 Agent | 🔥 最核心 |
| Phase 6 | 多步 Agent + 前端 | 1.5 周 | 研究助手 + Streamlit 界面 | ✅ |
| Phase 7 | (可选) 前端进阶 | 持续 | JS/TS → React 重写界面 | 🟡 后续目标 |

---

## 二、配套约定（我们俩怎么配合）

### 每次学习的节奏
1. **开始**：告诉我"今天做 Phase X，Step Y"，我会给你讲解 + 代码骨架。
2. **中间**：你照着敲，遇到报错直接贴给我，我帮你 debug 并解释"为什么错"。
3. **结束**：完成一个 Step 就 Git commit；完成一个 Phase 告诉我，我帮你 review + 验收。

### Git 约定
- 每个项目一个仓库（或一个大仓库里多文件夹，随你）
- commit message 用中文写清楚：`feat: 完成翻译接口的基本逻辑`
- 每个 Phase 结束打个 tag：`git tag phase1-done`

### 进度跟踪
打开这份文档最下面的 **「📈 进度记录」** 表格，每完成一个 Phase 就回来更新一下。

---

## Phase 0：环境搭建 + Git 热身（1-2 天）

### 🎯 目标
把开发环境跑通，跑出第一个 FastAPI 程序，建立"改代码 → 刷新页面 → 看到变化"的正反馈循环。

### 📖 官方文档章节（FastAPI 中文文档）
- 安装 → https://fastapi.tiangolo.com/zh/tutorial/#_3
- **First Steps** → https://fastapi.tiangolo.com/zh/tutorial/first-steps/
- **Path Parameters**（只看前半，建立概念）→ https://fastapi.tiangolo.com/zh/tutorial/path-params/

### ✅ 分步任务

#### Step 0.1　装好工具链
- [ ] 安装 **Python 3.11+**（`python --version` 能跑）
- [ ] 安装 **VS Code** + 扩展：Python、Pylance、autoDocstring
- [ ] 安装 **Git**（你应该有了）
- [ ] 注册 **DeepSeek** 并拿到 API Key（https://platform.deepseek.com/ → 充 10 元够你用整个暑假）
- [ ] （可选但强烈推荐）装一个 **API 测试工具**：VS Code 里装 **Thunder Client** 扩展，或者用 VS Code REST Client

#### Step 0.2　搞懂"虚拟环境"为什么必须
- [ ] 在本文件夹创建虚拟环境：`python -m venv .venv`
- [ ] 激活（Windows cmd）：`.venv\Scripts\activate.bat`
- [ ] 在 GitHub 建一个仓库 `fastapi-agent-learning`，把本文件夹推上去
- [ ] 写一个 `.gitignore`（内容至少包含 `.venv/`、`__pycache__/`、`.env`）——**我可以直接给你**

#### Step 0.3　跑出第一个 FastAPI
- [ ] `pip install fastapi "uvicorn[standard]"`
- [ ] 新建 `main.py`，写一个返回 `{"hello": "world"}` 的接口
- [ ] 运行：`uvicorn main:app --reload`
- [ ] 浏览器打开 http://127.0.0.1:8000，再打开 http://127.0.0.1:8000/docs —— **看到自动生成的 API 文档，你会觉得很爽**
- [ ] 修改代码看页面自动刷新（`--reload` 的作用）
- [ ] Git commit：`feat(phase0): 第一个 FastAPI 程序跑通`

### 🏁 通关标准
- [ ] `http://127.0.0.1:8000/docs` 能打开，能看到你的接口
- [ ] `.venv` 没有被提交到 GitHub（检查仓库里没有 `.venv` 文件夹）
- [ ] 能用自己的话说出"虚拟环境"和 `--reload` 各解决什么问题

---

## Phase 1：FastAPI 基础（1 周）

### 🎯 目标
掌握 FastAPI 的四大基石：**路由参数 / 请求体 / pydantic 校验 / 响应模型**。做出一个内存版"个人记账 API"。

### 📦 项目背景：个人记账 API
记录每一笔开销，支持分类、备注、时间。Phase 1 数据存在内存里（Python list），Phase 2 再换成数据库。

```
📋 数据模型 Expense（一笔开销）
  id:          int       （自增）
  amount:      float     （金额，必须 > 0）
  category:    str       （分类：餐饮/交通/娱乐/学习/其他）
  note:        str       （备注，可选，最长 100 字）
  paid_at:     datetime  （消费时间，默认现在）

🔌 接口一览（Phase 1 先做前 5 个）
  POST   /expenses         记一笔
  GET    /expenses         列表（支持 ?category=餐饮 筛选）
  GET    /expenses/{id}    查单笔
  PUT    /expenses/{id}    修改
  DELETE /expenses/{id}    删除
  ────── 以下 Phase 1 先不做，Phase 2 再加 ──────
  GET    /summary/total    总支出
  GET    /summary/category 分类汇总（饼图数据）
```

### 📖 官方文档章节（按顺序学）
1. **Path Parameters**（路径参数）→ https://fastapi.tiangolo.com/zh/tutorial/path-params/
2. **Query Parameters**（查询参数）→ https://fastapi.tiangolo.com/zh/tutorial/query-params/
3. **Request Body**（请求体）→ https://fastapi.tiangolo.com/zh/tutorial/body/
4. **Query Parameters and String Validations**（校验）→ https://fastapi.tiangolo.com/zh/tutorial/query-params-str-validations/
5. **Path Parameters and Numeric Validations** → https://fastapi.tiangolo.com/zh/tutorial/path-params-numeric-validations/
6. **Body - Fields**（字段校验）→ https://fastapi.tiangolo.com/zh/tutorial/body-fields/
7. **Body - Nested Models**（嵌套模型）→ https://fastapi.tiangolo.com/zh/tutorial/body-nested-models/
8. **Extra Data Types**（日期时间类型，记账 API 必用）→ https://fastapi.tiangolo.com/zh/tutorial/extra-data-types/
9. **Response Model**（响应模型）⭐ 重要 → https://fastapi.tiangolo.com/zh/tutorial/response-model/

### ✅ 分步任务

#### Step 1.1　路径参数 + 查询参数
- [ ] 跟着文档写：`/items/{item_id}` 接口，理解路径参数
- [ ] 写一个带查询参数的接口，理解 `?key=value` 的写法
- [ ] **理解练习**：把某个查询参数设成 `Optional`（可空），打开 `/docs` 看差别

#### Step 1.2　请求体（Request Body）+ pydantic
- [ ] 定义 `ExpenseCreate` 模型：`amount`, `category`, `note`, `paid_at`
- [ ] 写一个 `POST /expenses` 接口接收它
- [ ] 打开 `/docs` 点 "Try it out" 测试，感受 pydantic 自动校验
- [ ] **理解练习**：故意传 `amount: "abc"`，看返回的错误信息长啥样

#### Step 1.3　字段校验 + 枚举 + 日期
- [ ] `amount` 加约束：`Field(gt=0)`（必须大于 0）
- [ ] `category` 用 `Enum` 限制只能选 5 个分类，传错自动报错
- [ ] `note` 加约束：`Field(default="", max_length=100)`
- [ ] `paid_at` 用 `datetime` 类型，设置 `default_factory=lambda: datetime.now()`

#### Step 1.4　响应模型（response_model）
- [ ] 理解"为什么要有 response_model"（入参模型和出参模型分开设计）
- [ ] 定义 `ExpenseOut`（比 `ExpenseCreate` 多一个 `id`）
- [ ] 所有接口用 `response_model=ExpenseOut` 规范返回

#### Step 1.5　📦 小项目：记账 API（内存版）
做一个完整的 CRUD，数据存在模块级 `list` 里：
- [ ] 用一个自增计数器生成 `id`
- [ ] `GET /expenses` 支持 `?category=餐饮` 和 `?skip=0&limit=10` 分页
- [ ] `GET /expenses/{id}` 找不到时返回 404
- [ ] `PUT /expenses/{id}` 支持部分更新（只改传了的字段）
- [ ] `DELETE /expenses/{id}`
- [ ] 用 Thunder Client / `/docs` 把所有接口都测一遍
- [ ] Git commit + tag `phase1-done`

### 🏁 通关标准
- [ ] 能用自己的话解释：路径参数 vs 查询参数 vs 请求体的区别
- [ ] 能说出 pydantic 在帮你做哪两件事（校验 + 转换类型）
- [ ] 能解释为什么入参用 `ExpenseCreate`、出参用 `ExpenseOut` 分开
- [ ] 记账 API 的 5 个接口都能正常工作

---

## Phase 2：FastAPI 进阶 + 数据库（1 周）

### 🎯 目标
学会**持久化**（让数据存下来），掌握**异步**和**依赖注入**——这两个是后面写 Agent 的关键。加上**统计接口**，让记账 API 有"数据分析"的味道。

### 📖 官方文档章节
1. **Handling Errors**（异常处理）→ https://fastapi.tiangolo.com/zh/tutorial/handling-errors/
2. **Path Operation Configuration**（status_code、tags、summary）→ https://fastapi.tiangolo.com/zh/tutorial/path-operation-configuration/
3. **Dependencies**（依赖注入）⭐⭐ → https://fastapi.tiangolo.com/zh/tutorial/dependencies/
   - 子依赖 → https://fastapi.tiangolo.com/zh/tutorial/dependencies/sub-dependencies/
   - 带 yield 的依赖 → https://fastapi.tiangolo.com/zh/tutorial/dependencies/dependencies-with-yield/
4. **Middleware**（中间件）→ https://fastapi.tiangolo.com/zh/tutorial/middleware/
5. **CORS**（跨域，后面接前端要用）→ https://fastapi.tiangolo.com/zh/tutorial/cors/
6. **Bigger Applications**（项目结构，拆分文件 + APIRouter）→ https://fastapi.tiangolo.com/zh/tutorial/bigger-applications/
7. **SQL (Relational) Databases**（用 SQLAlchemy）→ https://fastapi.tiangolo.com/zh/tutorial/sql-databases/
8. **Async / Await**（异步，建议反复读）→ https://fastapi.tiangolo.com/zh/async/

### ✅ 分步任务

#### Step 2.1　依赖注入（DI）—— 写 Agent 时天天用
- [ ] 写一个依赖：从 Header 里读 `X-Token`，没传或不对就抛 401（模拟鉴权）
- [ ] 把它挂到某个接口上，理解 `Depends(token_validator)` 的写法
- [ ] 理解"依赖可以嵌套"：A 依赖 B，B 依赖 C
- [ ] 理解 `Depends()` 的复用机制（同一个依赖一次请求只执行一次）

#### Step 2.2　异常处理 + 中间件 + 接口配置
- [ ] 自定义 `ExpenseNotFound` 异常，用 `@app.exception_handler` 统一处理成 404
- [ ] 用 **Path Operation Configuration**：给记账接口加 `tags=["记账"]`、`summary`、`status_code=201`
- [ ] 写一个中间件，记录每个请求的耗时（打印到终端）

#### Step 2.3　异步 async/await ⭐ Agent 的命脉
- [ ] 理解 `async def` 和 `def` 的区别，以及 FastAPI 何时用哪个
- [ ] 理解"阻塞操作会卡住整个服务器"——这是后面调用 LLM 时的关键坑
- [ ] **重要练习**：用 `httpx.AsyncClient` 异步请求一个公开 API（比如 https://httpbin.org/get）

#### Step 2.4　数据库（SQLite + SQLAlchemy）
- [ ] 把 Phase 1 的内存 list 换成 SQLite（零配置，一个文件）
- [ ] 学会：建表 ORM model、session、CRUD 操作
- [ ] 把数据库 session 用"依赖注入 + yield"的方式管理（这是 FastAPI 的经典模式）
- [ ] 验证：重启服务器，数据还在

#### Step 2.5　项目结构（拆分文件 + APIRouter）
- [ ] 把代码拆成：`models.py`（ORM 表）/ `schemas.py`（pydantic）/ `database.py`（连接）/ `crud.py`（数据库操作）/ `main.py`（入口）
- [ ] 用 **APIRouter** 把记账路由拆到 `routers/expenses.py`
- [ ] 加上 CORS 配置（为后面接前端做准备）

#### Step 2.6　⭐ 统计接口（记账 API 的灵魂）
这一步让项目从"CRUD 练习"变成"有点意思的小产品"：
- [ ] `GET /summary/total` —— 总支出（支持 `?start=2026-06-01&end=2026-06-30` 按时间段）
- [ ] `GET /summary/category` —— 各分类占比，返回格式适合画饼图：
  ```json
  [ {"category": "餐饮", "amount": 520.5, "count": 12},
    {"category": "交通", "amount": 180.0, "count": 5} ]
  ```
- [ ] （加分）`GET /summary/daily` —— 按天汇总，返回每天的支出，适合画折线图
- [ ] 学会 SQL 的 `GROUP BY` / `SUM` / `COUNT` 聚合查询（或 SQLAlchemy 写法）

### 🏁 通关标准
- [ ] 能解释"为什么调用 LLM 这种慢操作必须用异步"
- [ ] 数据重启不丢失
- [ ] 代码按功能拆分到了不同文件，用了 APIRouter
- [ ] 统计接口能正确算出总支出和分类占比
- [ ] Git tag `phase2-done`

---

## Phase 3：LLM 接入 + Prompt 工程（1 周）

### 🎯 目标
第一次让 FastAPI 和大模型"对话"。做出一个**翻译 API** 和一个**智能改写 API**。

### 📖 学习资料
- DeepSeek API 文档（OpenAI 兼容）→ https://api-docs.deepseek.com/
- OpenAI SDK 用法（DeepSeek 复用同一套）→ https://platform.openai.com/docs/api-reference
- Prompt 工程指南 → https://www.promptingguide.ai/zh

### ✅ 分步任务

#### Step 3.1　调通 DeepSeek
- [ ] `pip install openai python-dotenv`
- [ ] 把 API Key 放 `.env`：`DEEPSEEK_API_KEY=sk-xxx`
- [ ] 写一个 `llm.py`，用 `openai` SDK 连 DeepSeek（`base_url="https://api.deepseek.com"`）
- [ ] 在终端跑通：发一句话，拿到回复

#### Step 3.2　翻译 API
- [ ] `POST /translate` 接收 `{text, target_lang}`，返回翻译结果
- [ ] 用 pydantic 校验入参；用 `response_model` 规范出参
- [ ] 用**异步**调用 LLM（`AsyncOpenAI`）—— 回想 Phase 2.3
- [ ] 处理 LLM 超时、报错（异常处理复用 Phase 2.2）

#### Step 3.3　Prompt 工程实战
- [ ] 做一个 `POST /rewrite` 接口：把一段话改成"正式/幽默/简洁"三种风格
- [ ] 练习：system prompt vs user prompt 的区别
- [ ] 练习：让输出**严格返回 JSON**（用 `response_format={"type": "json_object"}`），pydantic 再校验一遍
- [ ] 练习：温度（temperature）参数对结果的影响

#### Step 3.4　📦 小项目：智能客服 FAQ（规则版）
- [ ] 一个 `/chat` 接口，带系统提示词设定"你是 XX 公司的客服，只回答 XX 领域的问题"
- [ ] 加上多轮对话支持（维护 messages 历史）
- [ ] 用 Streamlit（10 行代码）套个聊天界面：`pip install streamlit`

### 🏁 通关标准
- [ ] API Key 绝对没出现在代码里、没提交到 Git（检查！）
- [ ] 能解释 system / user / assistant 三种消息的区别
- [ ] 能让 LLM 稳定返回 JSON
- [ ] Streamlit 聊天界面能跑

---

## Phase 4：RAG 知识库（1 周）

### 🎯 目标
让 Agent 有"记忆"和"知识"。做出一个**能回答你自己文档问题的机器人**。

### 📖 学习资料
- RAG 概念 → https://www.promptingguide.ai/zh/techniques/rag
- 向量库 Chroma 文档 → https://docs.trychroma.com/
- Embedding 概念（搜 "embedding 是什么"）

### ✅ 分步任务

#### Step 4.1　理解 RAG 流程
- [ ] 画一张图（自己手画）：文档 → 切片 → embedding → 存向量库；提问 → embedding → 检索 → 拼 prompt → LLM 生成
- [ ] 能解释：为什么不直接把整本 PDF 塞给 LLM？（上下文长度、成本、幻觉）

#### Step 4.2　文本切片 + Embedding
- [ ] `pip install chromadb`
- [ ] 用 DeepSeek/OpenAI 的 embedding 接口（或本地 sentence-transformers）
- [ ] 把一份文档（比如某个产品的说明书 .txt）切成段落
- [ ] 生成 embedding 存进 Chroma

#### Step 4.3　检索 + 生成
- [ ] 写一个函数：`retrieve(question) -> top_k_chunks`
- [ ] 写一个函数：`answer(question, chunks) -> response`（把 chunks 塞进 prompt）
- [ ] 组装成 `POST /ask` 接口

#### Step 4.4　📦 小项目：文档问答机器人
- [ ] 给它喂一份 PDF（用 PyPDF2 或 pdfplumber 提取文字——这里可以用我提供的 PDF 工具能力）
- [ ] 加个"引用来源"功能：回答时标注"来自第 X 段"
- [ ] Streamlit 界面：上传 PDF → 提问 → 显示答案 + 来源

### 🏁 通关标准
- [ ] 能解释 embedding 和向量检索的直觉含义
- [ ] 换一份新文档，机器人能正确回答
- [ ] Git tag `phase4-done`

---

## Phase 5：⭐ Agent 核心（1.5 周，最关键）

### 🎯 目标
**这是整个计划的核心。** 做出第一个真正意义上的 Agent——它会自己决定"调用哪个工具"。

### 📖 学习资料
- Function Calling 概念 → DeepSeek / OpenAI 文档的 "Function Calling" 章节
- ReAct 论文思想（think → act → observe 循环）→ 搜 "ReAct reasoning acting LLM"
- ⚠️ **这一阶段先不学 LangChain**，全部手写，理解底层。学完 Phase 5 再看框架。

### ✅ 分步任务

#### Step 5.1　理解 Agent 的本质
- [ ] 能解释：Agent = LLM + 工具 + 循环
- [ ] 能解释：和普通 LLM 对话的区别是什么（"它能动嘴，还能动手"）
- [ ] 能解释：ReAct 循环的四个步骤

#### Step 5.2　定义工具（Function / Tool）
- [ ] 手写 3 个简单工具（纯 Python 函数）：
  - `get_weather(city)` —— 返回模拟天气
  - `calculate(expression)` —— 用 `eval` 算数学题（注意安全，只允许数字和运算符）
  - `search_db(keyword)` —— 从 Phase 4 的向量库搜
- [ ] 把它们包装成 OpenAI Function Calling 需要的 JSON schema 格式

#### Step 5.3　单次工具调用
- [ ] 写个 `POST /agent` 接口：
  - 用户问"北京天气怎么样"
  - LLM 决定调用 `get_weather`
  - 你执行 `get_weather("北京")`，把结果喂回 LLM
  - LLM 生成最终回答

#### Step 5.4　⭐ ReAct 循环（多步）
- [ ] 把 Step 5.3 包进一个**循环**里：LLM 可能要连续调用多个工具（先查天气，再算温差）
- [ ] 设置最大循环次数（防止死循环）
- [ ] 每一步都打印 think / act / observe，观察 Agent 的"思考过程"

#### Step 5.5　📦 小项目：能干活的 Agent
- [ ] 加更多工具：读文件、查时间、调你的翻译 API（复用 Phase 3）
- [ ] 做个任务：让 Agent "把今天的天气翻译成英文并算出华氏度"——看它自己组合工具
- [ ] Streamlit 界面：展示 Agent 每一步的思考过程

### 🏁 通关标准
- [ ] **不看任何框架，纯手写**出一个能用工具的 Agent
- [ ] 能解释"为什么需要循环、为什么需要限制次数"
- [ ] 你已经超越了 80% 只会用 LangChain 的人 ✊
- [ ] Git tag `phase5-done` —— **庆祝一下**

---

## Phase 6：多步 Agent + 前端（1.5 周）

### 🎯 目标
做一个**完整的小产品**：多步骤的研究助手 Agent + 像样的界面。

### 📖 学习资料
- LangGraph 文档（到这里可以看了）→ https://langchain-ai.github.io/langgraph/
- Streamlit 进阶 → https://docs.streamlit.io/
- MCP 协议（前沿，了解即可）→ https://modelcontextprotocol.io/

### ✅ 分步任务

#### Step 6.1　用 LangGraph 重写 Phase 5 的 Agent
- [ ] 对比：手写 vs 框架，理解框架帮你省了什么
- [ ] 用 LangGraph 的状态图（StateGraph）重新编排

#### Step 6.2　多步任务 Agent
- [ ] 做一个"研究助手"：给个题目 → 搜索 → 阅读 → 总结 → 生成报告
- [ ] 学会：状态管理、条件分支、人工审核节点（human-in-the-loop）

#### Step 6.3　📦 小项目：研究助手产品
- [ ] Streamlit 界面：输入题目 → 实时展示 Agent 进度 → 导出报告
- [ ] 加 Docker（写个 Dockerfile）→ 这一步学会了，部署能力就有了
- [ ] （可选前沿）把工具包装成 MCP Server，用 Claude Desktop 连

### 🏁 通关标准
- [ ] 有一个能演示给别人看的完整产品
- [ ] Git tag `phase6-done`

---

## Phase 7：（后续目标）前端进阶 —— JS/TS → React

> 这是你说的"目前不作为首要目标"，放在 8 周主线之后。等你 Phase 6 结束，再用这个路线把 Streamlit 界面升级成正式的 React 前端。

### 推荐路线
1. **JavaScript 基础**（2 周）→ MDN 教程 https://developer.mozilla.org/zh-CN/docs/Learn/JavaScript
   - 重点：变量、函数、数组/对象方法、异步（Promise / async-await，和 Python 的很像）、DOM
2. **TypeScript**（1 周）→ 官方 Handbook https://www.typescriptlang.org/zh/docs/
   - 重点：类型注解（你 pydantic 已经会了，概念相通）、interface、泛型
3. **React**（3-4 周）→ 官方文档（新版很好）https://zh-hans.react.dev/learn
   - 重点：组件、props、useState/useEffect、事件处理
4. **Next.js + Tailwind**（2 周）→ https://nextjs.org/learn
   - 这是当前主流的"全栈前端"方案
5. **回头给 Phase 6 的 Agent 做个 React 界面**，用 `fetch` 调你的 FastAPI 接口（记得 Phase 2 的 CORS）

---

## 📈 进度记录（自己更新）

| 阶段 | 计划完成日期 | 实际完成日期 | 自评（1-10） | 笔记/踩坑 |
|------|------------|------------|------------|----------|
| Phase 0 | ___/___ | | | |
| Phase 1 | ___/___ | | | |
| Phase 2 | ___/___ | | | |
| Phase 3 | ___/___ | | | |
| Phase 4 | ___/___ | | | |
| Phase 5 | ___/___ | | | |
| Phase 6 | ___/___ | | | |

---

## 💡 随时可以问老师的问题

卡住的时候，直接问我（把我当助教）：
- "Phase 2 Step 2.3，我不理解 async 和 sync 到底差在哪，能不能给个会卡死的例子？"
- "我的代码报了这个错 [贴报错]，帮我看看"
- "这个概念我用比喻理解对不对：[你的比喻]"
- "我做完了 Phase X，帮我 review 一下代码"
- "下一步该做哪个 Step？"

---

## 🎯 8 周后你应该能：
- ✅ 独立用 FastAPI 写出规范、带校验、有数据库的 API
- ✅ 理解并能手写一个 Agent（LLM + 工具 + 循环）
- ✅ 会 RAG，能让 LLM 基于你的文档回答问题
- ✅ 用 Streamlit 出演示界面
- ✅ GitHub 上有 5+ 个真实项目仓库
- ✅ 具备继续自学 LangGraph / MCP / React 的扎实基础

**我们开始吧 👊 —— 从 Phase 0 Step 0.1 开始？**
