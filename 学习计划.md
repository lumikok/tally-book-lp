# 从 FastAPI 到 Agent 工程师 —— 暑期学习计划

> 🧑‍🏫 老师：ZCode ｜ 👨‍🎓 学生：你
> 起始日期：2026-06-17（暑假）｜ 节奏：每天 2-3 小时有效学习
> 核心原则：**边学边做，每个知识点配一个小项目，做完再学下一个。不追求"学完"，追求"做出来"。**

---

## 〇、学习心法（先读这一段，比后面的任务更重要）

1. **别"看完文档再动手"**：官方文档每个例子都敲一遍、改一改，比"通读"有用 10 倍。
2. **小步快跑**：每个 Step 都让你立刻看到结果（哪怕只是终端里打印一行）。
3. **遇到不懂的概念先问 AI 老师，再搜文档**：节省时间，别在某个名词上卡半天。
4. **每个项目都要 Git commit + 推到 GitHub**：暑期结束你会有多个真实仓库，这是你简历上最硬的东西。
5. **写"学习笔记 + 踩坑记录"**：每周一次，讲清楚 = 真懂了。

---

## 一、技能全景图（暑期结束你会点亮这些）

```
                         [ 你现在的位置 ]
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
   Phase 1-2               Phase 3                Phase 4-6
  后端 + 数据库             前端可视化            LLM + Agent
        │                      │                      │
   Python 进阶          原生 HTML/CSS/JS         DeepSeek API
   路由/校验             fetch / DOM / 事件       Prompt 工程
   pydantic             ECharts 图表             Function Calling
   异步 async            前后端联调(CORS)          ReAct 循环
   依赖注入                                       多步任务编排
   数据库(SQLite)                                  RAG 知识库
```

| 阶段    | 主题                      | 时长   | 产出项目                    | 状态        |
| ------- | ------------------------- | ------ | --------------------------- | ----------- |
| Phase 0 | 环境搭建 + Git 热身       | 1-2 天 | 仓库 + Hello World          | ✅ 完成     |
| Phase 1 | FastAPI 基础              | 1 周   | 记账 API（内存版）          | ✅ 完成     |
| Phase 2 | FastAPI 进阶 + 数据库     | 1 周   | 记账 API（持久化 + 统计版） | 🔄 进行中   |
| Phase 3 | ⭐ 前端可视化             | 1 周   | 记账 API 的网页界面 + 图表  | ⬜ 下一个   |
| Phase 4 | LLM 接入 + Prompt 工程    | 1 周   | 翻译 API / 智能客服         | ⬜          |
| Phase 5 | RAG 知识库                | 1 周   | 文档问答机器人              | ⬜          |
| Phase 6 | ⭐ Agent 核心             | 1.5 周 | 会用工具的 Agent            | ⬜ 最核心   |
| Phase 7 | (进阶) Agent 编排 / React | 持续   | LangGraph / JS→React        | 🟡 后续目标 |

> 💡 **顺序调整说明（2026-06-17）**：原计划把前端放在最后。现按你的要求调整为"记账 API 后端 → 前端可视化 → 再转 Agent"。好处是先把一个全栈小项目闭环，拿到成就感再开新方向。

---

## 二、配套约定（我们俩怎么配合）

### 每次学习的节奏

1. **开始**：告诉我"今天做 Phase X，Step Y"，我会给你讲解 + 代码骨架。
2. **中间**：你照着敲，遇到报错直接贴给我，我帮你 debug 并解释"为什么错"。
3. **结束**：完成一个 Step 就 Git commit；完成一个 Phase 告诉我，我帮你 review + 验收。

### Git 约定

- 远端仓库：https://github.com/lumikok/tally-book-lp
- commit message 用 Conventional Commits：`feat(phase1): Step1.3 ...`
- 每个 Phase 结束打个 tag：`git tag phase1-done`

### 进度跟踪

打开这份文档最下面的 **「📈 进度记录」** 表格，每完成一个 Phase 就回来更新一下。

---

## 三、📝 笔记方法论（贯穿全程，重要！）

你有两个笔记载体，分工明确，**别混着写**：

### 🔀 两类笔记，各归各位

| 笔记类型 | 放哪 | 写什么 | 举例 |
|---------|------|--------|------|
| **知识点笔记**<br>（你笔记网站里的 FastAPI / Python / C++ 等）| 笔记网站 | **可复用、跨项目的通用知识** | "pydantic 的 Field 约束有哪些参数"、"async/await 的语法"、"SQL 的 GROUP BY 用法" |
| **项目笔记**<br>（这个仓库里的 `notes/`）| 项目仓库 | **只属于这个项目的经验** | "tally-book 的 Category 枚举为什么继承 str"、"这次踩的坑：cmd 把 --oneline 敲成 --online" |

### 🧠 一句话决策法

学到一个点时，问自己：

> **"这个知识换个项目还用得上吗？"**

- **用得上**（比如 `Annotated` 的语法、`fetch` 怎么写）→ **知识点笔记**
- **只跟这个项目有关**（比如"我记账 API 的假数据怎么设计的"）→ **项目笔记**

> 💡 举个具体例子：你今天学了 `response_model`。
> - "response_model 的作用、语法、和返回类型注解的区别" → **FastAPI 知识点笔记**（换个项目还用）
> - "tally-book 里 ExpenseCreate 和 ExpenseOut 怎么拆的、踩了什么坑" → **项目笔记**（只跟这个项目有关）

### 📐 具体怎么做（分阶段）

#### 🔹 现阶段（Phase 0-2）：最小成本
- **项目笔记**：就用本计划的「📈 进度记录」表格的"笔记/踩坑"列，每个 commit 写一句话。**不开额外文件**。
- **知识点笔记**：学完一个知识点觉得"这个以后肯定常用"，**当天**就顺手记到笔记网站的对应页面。别攒着，攒着就忘。

#### 🔹 Phase 3 起：正式开项目笔记
- 在本仓库建 `notes/` 文件夹，每个 Phase 一个 `.md`：
  - `notes/phase2-后端进阶.md` —— 踩坑、设计决策、自己的理解
  - `notes/phase3-前端.md`
- 格式简单，分三块就行：**踩坑 / 关键概念（自己的话）/ 设计决策**

#### 🔹 Phase 4+：考虑写博客
学完一个完整方向（比如"我会 Agent 了"），写成文章发掘金/知乎。**教是最好的学**，也是简历加分项。

### ⚡ 三条铁律（违反就白记）

1. **绝不抄文档**：文档 10 秒能查到的东西，不记。只记"文档里没有的"——你的理解、你的坑、你的比喻。
2. **用自己的话**：能用比喻就别用术语。"response_model 像门卫，只放白名单字段出去" > 一堆官方定义。
3. **当天记**：顿悟时刻 24 小时内不记就蒸发。踩坑当天记，过两天就想不起细节了。

### 🔄 笔记和 commit 的关系

- **每次 commit**：在进度表"笔记/踩坑"列写一句话（项目笔记，最小成本）
- **每周一次**：把这一周攒的通用知识，整理进笔记网站的知识点笔记
- **每个 Phase 结束**：review 项目笔记，把"其实该归知识点"的条目搬到笔记网站

> 这套机制的核心：**项目里记过程，知识点库里记结论**。两边定期互相搬运，知识就沉淀下来了。

---

## Phase 0：环境搭建 + Git 热身 ✅ 已完成

### 🏁 通关成果

- 用 **uv** 代替 venv（更现代）
- `fastapi dev` 比 `uvicorn main:app --reload` 更优雅
- 第一个 FastAPI 程序跑通，`/docs` 自动文档可见
- Git 仓库初始化，远端绑定 `tally-book-lp`

---

## Phase 1：FastAPI 基础 ✅ 已完成

### 🏁 通关成果（tag: `phase1-done`）

- ✅ 路径参数 + 查询参数 + 请求体 三种入参方式
- ✅ pydantic 校验 + `Annotated` 现代写法
- ✅ 枚举类型（分类下拉菜单）
- ✅ `response_model` 入参出参模型分离
- ✅ `HTTPException` 错误处理
- ✅ 完整 CRUD（GET / POST / PUT / DELETE）

### 📝 已掌握 commit 历史

```
f1717e6  Phase 0  搭好 uv 环境
e45c70a  Step1.1  路径参数 + 查询参数
3894f43  Step1.2  请求体 + pydantic
6e23f1c  Step1.3  字段校验 + 枚举 + Annotated
ca3960b  Step1.4  响应模型 + HTTPException
c4680ba  Step1.5  完整 CRUD（tag: phase1-done）
```

---

## Phase 2：FastAPI 进阶 + 数据库（1 周）🔄 进行中

### 🎯 目标

学会**持久化**（让数据存下来），掌握**异步**和**依赖注入**——这两个是后面写 Agent 的关键。加上**统计接口**，为 Phase 3 前端图表提供数据。

### 📖 官方文档章节

1. **Handling Errors**（异常处理）→ https://fastapi.tiangolo.com/zh/tutorial/handling-errors/
2. **Path Operation Configuration**（status_code、tags、summary）→ https://fastapi.tiangolo.com/zh/tutorial/path-operation-configuration/
3. **Dependencies**（依赖注入）⭐⭐ → https://fastapi.tiangolo.com/zh/tutorial/dependencies/
   - 子依赖 → https://fastapi.tiangolo.com/zh/tutorial/dependencies/sub-dependencies/
   - 带 yield 的依赖 → https://fastapi.tiangolo.com/zh/tutorial/dependencies/dependencies-with-yield/
4. **Middleware**（中间件）→ https://fastapi.tiangolo.com/zh/tutorial/middleware/
5. **CORS**（跨域，Phase 3 接前端要用）→ https://fastapi.tiangolo.com/zh/tutorial/cors/
6. **Bigger Applications**（项目结构，拆分文件 + APIRouter）→ https://fastapi.tiangolo.com/zh/tutorial/bigger-applications/
7. **SQL (Relational) Databases**（用 SQLAlchemy）→ https://fastapi.tiangolo.com/zh/tutorial/sql-databases/
8. **Async / Await**（异步，建议反复读）→ https://fastapi.tiangolo.com/zh/async/

### ✅ 分步任务

#### Step 2.1　依赖注入（DI）—— 写 Agent 时天天用

- [ ] 写一个依赖：从 Header 里读 `X-Token`，没传或不对就抛 401（模拟鉴权）
- [ ] 把它挂到某个接口上，理解 `Depends(token_validator)` 的写法
- [ ] 理解"依赖可以嵌套"：A 依赖 B，B 依赖 C
- [ ] 理解 `Depends()` 的复用机制（同一个依赖一次请求只执行一次）

#### Step 2.2　异常处理 + 中间件 + 接口配置

- [ ] 自定义 `ExpenseNotFound` 异常，用 `@app.exception_handler` 统一处理成 404
- [ ] 用 **Path Operation Configuration**：给记账接口加 `tags=["记账"]`、`summary`、`status_code=201`
- [ ] 写一个中间件，记录每个请求的耗时（打印到终端）

#### Step 2.3　异步 async/await ⭐ Agent 的命脉

- [ ] 理解 `async def` 和 `def` 的区别，以及 FastAPI 何时用哪个
- [ ] 理解"阻塞操作会卡住整个服务器"——这是后面调用 LLM 时的关键坑
- [ ] **重要练习**：用 `httpx.AsyncClient` 异步请求一个公开 API（比如 https://httpbin.org/get）

#### Step 2.4　数据库（SQLite + SQLAlchemy）

- [ ] 把 Phase 1 的内存 list 换成 SQLite（零配置，一个文件）
- [ ] 学会：建表 ORM model、session、CRUD 操作
- [ ] 把数据库 session 用"依赖注入 + yield"的方式管理（这是 FastAPI 的经典模式）
- [ ] 验证：重启服务器，数据还在

#### Step 2.5　项目结构（拆分文件 + APIRouter）

- [ ] 把代码拆成：`models.py`（ORM 表）/ `schemas.py`（pydantic）/ `database.py`（连接）/ `crud.py`（数据库操作）/ `main.py`（入口）
- [ ] 用 **APIRouter** 把记账路由拆到 `routers/expenses.py`
- [ ] 加上 CORS 配置（为 Phase 3 接前端做准备）

#### Step 2.6　⭐ 统计接口（记账 API 的灵魂，也是 Phase 3 图表的数据源）

这一步让项目从"CRUD 练习"变成"有点意思的小产品"：

- [ ] `GET /summary/total` —— 总支出（支持 `?start=2026-06-01&end=2026-06-30` 按时间段）
- [ ] `GET /summary/category` —— 各分类占比，返回格式适合画饼图：
  ```json
  [
    { "category": "餐饮", "amount": 520.5, "count": 12 },
    { "category": "交通", "amount": 180.0, "count": 5 }
  ]
  ```
- [ ] （加分）`GET /summary/daily` —— 按天汇总，返回每天的支出，适合画折线图
- [ ] 学会 SQL 的 `GROUP BY` / `SUM` / `COUNT` 聚合查询（或 SQLAlchemy 写法）

### 🏁 通关标准

- [ ] 能解释"为什么调用 LLM 这种慢操作必须用异步"
- [ ] 数据重启不丢失
- [ ] 代码按功能拆分到了不同文件，用了 APIRouter
- [ ] 统计接口能正确算出总支出和分类占比
- [ ] Git tag `phase2-done`

---

## Phase 3：⭐ 前端可视化（1 周）

### 🎯 目标

给记账 API 配一个**真正的网页界面**：能记一笔、看列表、增删改查，还能用 **ECharts** 画出分类饼图和趋势折线图。这一步让你**真正打通全栈**，并为以后学 React 打下 HTML/JS/CSS 基础。

### 🛠️ 技术栈

- **原生 HTML + CSS + JavaScript**（不依赖任何前端框架，先把基本功打扎实）
- **ECharts**（百度开源的图表库，一行 `<script>` 引入就能用，画饼图/折线图超简单）
- **fetch API**（浏览器原生，调你的 FastAPI 接口）

> 💡 为什么先不用框架？因为 React/Vue 这些框架封装了 HTML/JS 的细节，零基础直接学会"知其然不知其所以然"。先用原生 JS 懂了 DOM 和事件，再学框架才能游刃有余。ECharts 也比 React 简单太多，能快速给你"出图"的成就感。

### 📖 学习资料

- **JavaScript 基础**（MDN，重点章节）→ https://developer.mozilla.org/zh-CN/docs/Learn/JavaScript
  - 重点：变量、函数、数组/对象方法、`fetch`、DOM、事件监听
- **ECharts 5 分钟上手** → https://echarts.apache.org/handbook/zh/get-started/
- **fetch API** → https://developer.mozilla.org/zh-CN/docs/Web/API/fetch

### ✅ 分步任务

#### Step 3.1　前端三件套热身 + fetch 调通后端

- [ ] 在项目根目录建 `frontend/` 文件夹，放 `index.html` / `style.css` / `app.js`
- [ ] 用 VS Code 的 Live Server 扩展打开 `index.html`，学会"前端热刷新"
- [ ] 确认后端已开 CORS（Phase 2.5 做过）—— 否则前端调不通
- [ ] 写一个最小 `fetch("http://127.0.0.1:8000/expenses")`，把返回数据 `console.log` 出来
- [ ] 理解"跨域 CORS"和"同源策略"是怎么回事

#### Step 3.2　记一笔表单（POST）

- [ ] 用 HTML 写一个表单：金额、分类（下拉）、备注、日期
- [ ] JS 监听表单 `submit` 事件，用 `fetch` POST 到后端
- [ ] 成功后清空表单 + 刷新列表

#### Step 3.3　开销列表 + 增删改

- [ ] 用 `fetch` 拉取列表，用 JS 动态生成 `<tr>` 渲染表格
- [ ] 每行加"删除"按钮，点击 → `DELETE` 请求 → 刷新列表
- [ ] 每行加"编辑"按钮，点击 → 弹出表单预填 → `PUT` 请求 → 刷新
- [ ] 顶部加分类筛选 + 分页（复用后端的查询参数）

#### Step 3.4　⭐ ECharts 图表（可视化灵魂）

- [ ] 引入 ECharts：`<script src="https://cdn.jsdelivr.net/npm/echarts/dist/echarts.min.js"></script>`
- [ ] **饼图**：调 `/summary/category`，画"各分类支出占比"
- [ ] **折线图**：调 `/summary/daily`，画"每日支出趋势"
- [ ] （加分）**数字大屏**：顶部显示总支出、本月支出、最大单笔

#### Step 3.5　📦 小项目：记账网页应用

- [ ] 把表单、列表、图表整合成一个完整页面（可用 Tab 切换）
- [ ] 美化样式（用一点 CSS，或者引入一个轻量 CSS 框架如 Pico.css）
- [ ] 部署体验：把前端放到 FastAPI 的 `static/` 目录，一个端口访问（学 `StaticFiles`）

### 🏁 通关标准

- [ ] 能解释 fetch 是什么、为什么前后端分离需要 CORS
- [ ] 能用 JS 操作 DOM（增删改查页面元素）
- [ ] 能看懂 ECharts 的"option 配置"是怎么生成图的
- [ ] 有一个能给同学演示的记账网页
- [ ] Git tag `phase3-done`

---

## Phase 4：LLM 接入 + Prompt 工程（1 周）

### 🎯 目标

第一次让 FastAPI 和大模型"对话"。做出一个**翻译 API** 和一个**智能改写 API**。

### 📖 学习资料

- DeepSeek API 文档（OpenAI 兼容）→ https://api-docs.deepseek.com/
- OpenAI SDK 用法（DeepSeek 复用同一套）→ https://platform.openai.com/docs/api-reference
- Prompt 工程指南 → https://www.promptingguide.ai/zh

### ✅ 分步任务

#### Step 4.1　调通 DeepSeek

- [ ] `uv add openai python-dotenv`
- [ ] 把 API Key 放 `.env`：`DEEPSEEK_API_KEY=sk-xxx`（**别提交到 Git！**）
- [ ] 写一个 `llm.py`，用 `openai` SDK 连 DeepSeek（`base_url="https://api.deepseek.com"`）
- [ ] 在终端跑通：发一句话，拿到回复

#### Step 4.2　翻译 API

- [ ] `POST /translate` 接收 `{text, target_lang}`，返回翻译结果
- [ ] 用 pydantic 校验入参；用 `response_model` 规范出参
- [ ] 用**异步**调用 LLM（`AsyncOpenAI`）—— 回想 Phase 2.3
- [ ] 处理 LLM 超时、报错（异常处理复用 Phase 2.2）

#### Step 4.3　Prompt 工程实战

- [ ] 做一个 `POST /rewrite` 接口：把一段话改成"正式/幽默/简洁"三种风格
- [ ] 练习：system prompt vs user prompt 的区别
- [ ] 练习：让输出**严格返回 JSON**（用 `response_format={"type": "json_object"}`），pydantic 再校验一遍
- [ ] 练习：温度（temperature）参数对结果的影响

#### Step 4.4　📦 小项目：智能客服 FAQ（规则版）

- [ ] 一个 `/chat` 接口，带系统提示词设定"你是 XX 公司的客服，只回答 XX 领域的问题"
- [ ] 加上多轮对话支持（维护 messages 历史）
- [ ] 用 Phase 3 学的前端技能，给它配个聊天界面（复用 fetch）

### 🏁 通关标准

- [ ] API Key 绝对没出现在代码里、没提交到 Git（检查！）
- [ ] 能解释 system / user / assistant 三种消息的区别
- [ ] 能让 LLM 稳定返回 JSON
- [ ] Git tag `phase4-done`

---

## Phase 5：RAG 知识库（1 周）

### 🎯 目标

让 Agent 有"记忆"和"知识"。做出一个**能回答你自己文档问题的机器人**。

### 📖 学习资料

- RAG 概念 → https://www.promptingguide.ai/zh/techniques/rag
- 向量库 Chroma 文档 → https://docs.trychroma.com/
- Embedding 概念（搜 "embedding 是什么"）

### ✅ 分步任务

#### Step 5.1　理解 RAG 流程

- [ ] 画一张图（自己手画）：文档 → 切片 → embedding → 存向量库；提问 → embedding → 检索 → 拼 prompt → LLM 生成
- [ ] 能解释：为什么不直接把整本 PDF 塞给 LLM？（上下文长度、成本、幻觉）

#### Step 5.2　文本切片 + Embedding

- [ ] `uv add chromadb`
- [ ] 用 DeepSeek/OpenAI 的 embedding 接口（或本地 sentence-transformers）
- [ ] 把一份文档（比如某个产品的说明书 .txt）切成段落
- [ ] 生成 embedding 存进 Chroma

#### Step 5.3　检索 + 生成

- [ ] 写一个函数：`retrieve(question) -> top_k_chunks`
- [ ] 写一个函数：`answer(question, chunks) -> response`（把 chunks 塞进 prompt）
- [ ] 组装成 `POST /ask` 接口

#### Step 5.4　📦 小项目：文档问答机器人

- [ ] 给它喂一份 PDF（用 PyPDF2 或 pdfplumber 提取文字）
- [ ] 加个"引用来源"功能：回答时标注"来自第 X 段"
- [ ] 配个上传 PDF → 提问 → 显示答案 + 来源的网页界面

### 🏁 通关标准

- [ ] 能解释 embedding 和向量检索的直觉含义
- [ ] 换一份新文档，机器人能正确回答
- [ ] Git tag `phase5-done`

---

## Phase 6：⭐ Agent 核心（1.5 周，最关键）

### 🎯 目标

**这是整个计划的核心。** 做出第一个真正意义上的 Agent——它会自己决定"调用哪个工具"。

### 📖 学习资料

- Function Calling 概念 → DeepSeek / OpenAI 文档的 "Function Calling" 章节
- ReAct 论文思想（think → act → observe 循环）→ 搜 "ReAct reasoning acting LLM"
- ⚠️ **这一阶段先不学 LangChain**，全部手写，理解底层。学完 Phase 6 再看框架。

### ✅ 分步任务

#### Step 6.1　理解 Agent 的本质

- [ ] 能解释：Agent = LLM + 工具 + 循环
- [ ] 能解释：和普通 LLM 对话的区别是什么（"它能动嘴，还能动手"）
- [ ] 能解释：ReAct 循环的四个步骤

#### Step 6.2　定义工具（Function / Tool）

- [ ] 手写 3 个简单工具（纯 Python 函数）：
  - `get_weather(city)` —— 返回模拟天气
  - `calculate(expression)` —— 算数学题
  - `search_db(keyword)` —— 从 Phase 5 的向量库搜
- [ ] 把它们包装成 OpenAI Function Calling 需要的 JSON schema 格式

#### Step 6.3　单次工具调用

- [ ] 写个 `POST /agent` 接口：
  - 用户问"北京天气怎么样"
  - LLM 决定调用 `get_weather`
  - 你执行 `get_weather("北京")`，把结果喂回 LLM
  - LLM 生成最终回答

#### Step 6.4　⭐ ReAct 循环（多步）

- [ ] 把 Step 6.3 包进一个**循环**里：LLM 可能要连续调用多个工具
- [ ] 设置最大循环次数（防止死循环）
- [ ] 每一步都打印 think / act / observe，观察 Agent 的"思考过程"

#### Step 6.5　📦 小项目：能干活的 Agent

- [ ] 加更多工具：读文件、查时间、调你的翻译 API（复用 Phase 4）
- [ ] 做个任务：让 Agent "把今天的天气翻译成英文并算出华氏度"——看它自己组合工具
- [ ] 配个界面：展示 Agent 每一步的思考过程（复用 Phase 3 前端技能）

### 🏁 通关标准

- [ ] **不看任何框架，纯手写**出一个能用工具的 Agent
- [ ] 能解释"为什么需要循环、为什么需要限制次数"
- [ ] 你已经超越了 80% 只会用 LangChain 的人 ✊
- [ ] Git tag `phase6-done` —— **庆祝一下**

---

## Phase 7：（后续目标）Agent 编排 / React 前端

完成主线后，按兴趣选择深入方向：

### 方向 A：Agent 编排框架

1. **LangGraph**（状态图编排多步 Agent）→ https://langchain-ai.github.io/langgraph/
2. **MCP 协议**（把工具包装成 MCP Server，给 Claude Desktop 用）→ https://modelcontextprotocol.io/
3. 做一个"研究助手"：给题目 → 搜索 → 总结 → 生成报告

### 方向 B：React 前端进阶

1. **TypeScript**（1 周）→ https://www.typescriptlang.org/zh/docs/
   - 重点：类型注解（你 pydantic 已经会了，概念相通）、interface、泛型
2. **React**（3-4 周）→ https://zh-hans.react.dev/learn
   - 重点：组件、props、useState/useEffect、事件处理
3. **Next.js + Tailwind**（2 周）→ https://nextjs.org/learn
   - 主流"全栈前端"方案
4. **回头把 Phase 3 的记账页面用 React 重写**，感受框架 vs 原生的差别

---

## 📈 进度记录

| 阶段    | 计划完成日期 | 实际完成日期 | 自评（1-10） | 笔记/踩坑                                                          |
| ------- | ------------ | ------------ | ------------ | ------------------------------------------------------------------ |
| Phase 0 | 06/18        | 2026-06-17   | 8            | 用 uv 代替 venv；`fastapi dev` 比 `uvicorn` 优雅；记得 Ctrl+S 保存 |
| Phase 1 | 06/24        | 2026-06-17   | 9            | 用 Annotated 现代写法；模型继承复用；HTTPException 处理 404        |
| Phase 2 | 07/01        | 进行中       |              |                                                                    |
| Phase 3 | 07/08        |              |              |                                                                    |
| Phase 4 | 07/15        |              |              |                                                                    |
| Phase 5 | 07/22        |              |              |                                                                    |
| Phase 6 | 07/31        |              |              |                                                                    |

---

## 💡 随时可以问老师的问题

卡住的时候，直接问我（把我当助教）：

- "Phase 2 Step 2.3，我不理解 async 和 sync 到底差在哪，能不能给个会卡死的例子？"
- "我的代码报了这个错 [贴报错]，帮我看看"
- "这个概念我用比喻理解对不对：[你的比喻]"
- "我做完了 Phase X，帮我 review 一下代码"
- "下一步该做哪个 Step？"

---

## 🎯 暑期结束你应该能：

- ✅ 独立用 FastAPI 写出规范、带校验、有数据库的 API
- ✅ 用原生 HTML/CSS/JS + ECharts 做出带图表的全栈小应用
- ✅ 理解并能手写一个 Agent（LLM + 工具 + 循环）
- ✅ 会 RAG，能让 LLM 基于你的文档回答问题
- ✅ GitHub 上有多个真实项目仓库（tally-book-lp 等）
- ✅ 具备继续自学 LangGraph / MCP / React 的扎实基础
